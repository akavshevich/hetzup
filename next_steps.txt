Show existing snapshots for a server as a selectable list
Implement deleting snapshots

DONE:
Rewrite get_running_servers, move general logic to generic call_hetzner_api function
Upon receiving response, make clear to TS what structure and props it should have
Show active servers as a nicely formatted CLI output
Make servers scrollable and selectable with arrows?
Create a generic function for asking to select an option
Function that pulls available images from Hetzner
Function that retrieves a list of available server plans
Pagination: merge arrays from non- "meta" object keys
Pagination: implement recursion, include page query param
Clean up code by moving cumbersome types from function definitions
Combine running servers and snapshots to build a full list of servers
Show servers as a selectable list
Change the full server list to specify active and inactive servers, not server/snapshot type. Add all snapshot IDs to an array for each.
Add useful info, such as server cores, ram and disk, number of snapshots. Format it with chalk
Upon server selection, show options based on status
Implement saving server to snapshot
Add nice loading messages and animations
Implement deleting server
Implement spinning up server from a snapshot