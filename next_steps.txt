Server types often unavailable at a certain location or at all. Prompt reconfiguration on error 412
Save server type, location, IPs used and other useful info on each server upon inactivating
Implement listing of primary IPs
When spinning up from snapshot, use saved server info if available
Implement custom spin up from snapshot where these details can be changed
Add quick start / quick stop that reverses the latest inactivated / spun up server as a first main menu option


DONE:
Rewrite get_running_servers, move general logic to generic call_hetzner_api function
Upon receiving response, make clear to TS what structure and props it should have
Show active servers as a nicely formatted CLI output
Make servers scrollable and selectable with arrows?
Create a generic function for asking to select an option
Function that pulls available images from Hetzner
Function that retrieves a list of available server plans
Pagination: merge arrays from non- "meta" object keys
Pagination: implement recursion, include page query param
Clean up code by moving cumbersome types from function definitions
Combine running servers and snapshots to build a full list of servers
Show servers as a selectable list
Change the full server list to specify active and inactive servers, not server/snapshot type. Add all snapshot IDs to an array for each.
Add useful info, such as server cores, ram and disk, number of snapshots. Format it with chalk
Upon server selection, show options based on status
Implement saving server to snapshot
Add nice loading messages and animations
Implement deleting server
Implement spinning up server from a snapshot
Show existing snapshots for a server as a selectable list
Implement deleting snapshots
Format date in a snapshot list
Create a main menu
Implement revert to previous snapshot
Added sleep function for timing burst requests apart
Implement complete server removal (both server and snapshots)
Implement warning and confirmation for dangerous server actions
Implement UI for all server actions
Add confirmation for revert, stop without saving, complete removal, snapshot deletion
When stopping a server with no snapshots, clarify that and change confirmation to critical
Implement function that returns snapshot details by ID for a server, show date in prompts and spinners
Implement error UI
Replace all console logs with error throws handled via error UI
Implement config write for hetzner_config.json and servers.json
Implement config file validation
Implement config repairs
Implement getting available locations
Implement getting ssh keys
Implement initial API key configuration
Show root password upon server creation if no ssh keys configured